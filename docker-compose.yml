services:
  cloudops-sentinel:
    build: .
    container_name: cloudops-sentinel
    ports:
      - "5000:5000"
    environment:
      AWS_DEFAULT_REGION: ap-south-1
    volumes:
      - "${USERPROFILE}/.aws:/root/.aws:ro"
    restart: unless-stopped
